import { StyleSheet } from "react-native";

export default StyleSheet.create({
    button:{
        alignItems: 'center',
        borderRadius: 10,
        borderColor: 'black'
    },
    buttonText:{
        fontSize: 26,
    }
})